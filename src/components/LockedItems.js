// displays in card form each locked item from the table
// allows users to drag and drop
// can delete cards
// can re-reroll cards
// can modify individual stats or abilities
// expandable/collapseable
import React, { PureComponent } from 'react'
import PropTypes from 'prop-types'

export default class LockedItems extends PureComponent {
  static propTypes = {

  }

  render() {
    return (
      <div>
        Locked Items
      </div>
    )
  }
}
